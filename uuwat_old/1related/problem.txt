aaby
IzaaABy

z=parent/level/teir


translate into hex codes
Insertion inside of "z" at location 1,1 (a,a)
new table 1x2 (AxB) with the name of "y"

!INS = insertion
@coord = (x,y)
@format = (x,(by)x, name)

@cell(id)!INS(3x3,teir)&data(newID&data[table,column,row], .....)

host&paramArgs(page="", user="")...if a param isn't called, the content doesn't show..
IF called parameter, the table that uses that data knows its parent for the layout..on page()

What if page=home&userAages="a-q"...for this to work page must==home

table (userAages == page=home+layout type)
	locationOnPage(teir,AbyB)


____on main layout table idea... + ifArgsTrue (page=home, userAge=f, loggedin=1)

on a new page where data is persistent...if a child's parent eleents are all empty, erase the teir and move the child up 1



______

ON page template type a1(default pageType for now) the layout is a jumble...

arguments like &userAages ....the table knows it only applies to page&pageType

I'm a new user. I created my mainpage template. Now, I want an about page for a few of my users.

option(warning modifying this template will modify all others, unless you want a template/look specific to this page&args)
WHen I make the about page, I have the option to build from one of my existing templates or start a new one.
I pick, build from existing template. All the entries from my mainpage template are now copied into a new table 
called "aboutTemplate"

I exit tempate making mode, and select new...(pagetype) About page for...(user(static), or blank) brandon!!(templates can be made to require arguments or not require arguments) I can write static boxes under aboutCOntent or, I can have the option to (create new datalist) I want to create a new datalist call "user about paragraphs" and the user Brandon contains this "text"
...or instead of having the page require arguments...just some elements inside it do...they wont' show if argument is missing
but the rest of the page will still render..or have default value that redirects to a parameter

//uuwat template code
uuwat_id style_classes parent page(s) content_table content_row content_column

Template = DL

id parent style content....thoguth process later(About template, how does it call nonspecific data from userAage table..args?)
\...the layout is already made, boxes and iheritance already set
templateCellid contentTable() whereContent()