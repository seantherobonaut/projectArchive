


try passing aonymous function as a paramenter

myfunction(asdf, function(){helloworld();});

object oriented programming! (why I'm starting to really like it!)

myvar asdf = new mainFunc()

asdf.countStuff();

asdf.display();


mainFunc()
{
	//variables, constructors
	countStuff()
	{
		//Hello world!
		//variables, code
		//return
	}

	display()
	{
		//Hello world!
		//variables, code
		//return
	}
}



===========================================================



and a pre and post function to leftrightAdapt

create a run once function




call the run once function to call calculate combined width

example:

leftrightAdapt(runOnce(calcCombinedWidth), "#navlogo", "#navlinks", 30, navbarAdapt);

try funct(a, function(){foo(x)});

some more to look at:

// callback function
function tryMe (param1, param2) { 
    alert (param1 + " and " + param2); 
} 

// callback executer 
function callbackTester (callback) { 
    callback(); 
} 

// test function
callbackTester (function() {
    tryMe("hello", "goodbye"); 
}); 