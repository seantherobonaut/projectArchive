Common practices:
make sure the back button works
the page should work without javascript

there is an ifIE html5 script? do they do css3 as well?

you can prevent anchor tags from going anywhere like this:
$("nav").delegate("a", "click", function()
{
return false;
});